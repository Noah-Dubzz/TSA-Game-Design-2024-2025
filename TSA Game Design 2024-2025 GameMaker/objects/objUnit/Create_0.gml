x_previous = x;

/// Create Event - objUnit

carrying_resource = false; // Initialize carrying resource
target_resource = noone;   // Ensure target_resource is also initialized

// Example initialization for Gatherers
    attack_timer = 0;  // Ensures attack logic works properly
    attack_speed = 30; // Adjust based on desired timing
    attack_damage = 1; // Damage dealt to resources
